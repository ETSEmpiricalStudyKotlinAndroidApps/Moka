<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="io.github.tonnyl.moka.ui.explore.ExploreViewModel" />

        <variable
            name="viewModel"
            type="ExploreViewModel" />

    </data>

    <LinearLayout
        android:id="@+id/fragment_explore_filter_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:theme="@style/AppTheme">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:elevation="4dp"
            android:theme="@style/AppTheme.AppBarOverlay"
            app:liftOnScroll="true"
            app:liftOnScrollTargetViewId="@id/recycler_view">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:theme="@style/AppTheme.Toolbar"
                app:navigationIcon="@drawable/ic_close_24"
                app:popupTheme="@style/AppTheme.PopupOverlay"
                tools:title="Filters">

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/toolbar_done"
                    style="@style/TextAppearance.MaterialComponents.Button"
                    android:layout_width="wrap_content"
                    android:layout_height="@dimen/regular_icon_size"
                    android:layout_gravity="end|center_vertical"
                    android:layout_marginEnd="@dimen/fragment_content_padding_half"
                    android:gravity="center"
                    android:onClick="@{() -> viewModel.confirmSelection()}"
                    android:paddingStart="@dimen/regular_icon_padding"
                    android:paddingEnd="@dimen/regular_icon_padding"
                    android:text="@string/explore_filter_done"
                    android:textColor="@color/colorPrimary" />

            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            app:layoutManager="LinearLayoutManager"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            tools:listitem="@layout/item_trending_language" />

    </LinearLayout>

</layout>