fragment CommitListItem on Commit {
    message
    messageHeadline
    committedDate
    committer {
        ...GitActorListItem
    }
    author {
        ...GitActorListItem
    }
    statusCheckRollup {
        state
    }
}