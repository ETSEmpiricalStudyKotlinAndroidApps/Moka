<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_screen_logo</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/AppTheme.PostSplashScreenTheme</item>
    </style>

    <style name="AppTheme.PostSplashScreenTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splashBackground</item>
        <item name="android:statusBarColor">@color/splashBackground</item>
        <item name="android:navigationBarColor">@color/splashBackground</item>
    </style>

    <style name="ContributionCalendarWeek">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:divider">@drawable/divider_contribution_calendar_day_widget</item>
        <item name="android:gravity">center</item>
        <item name="android:orientation">vertical</item>
        <item name="android:showDividers">middle</item>
        <item name="android:paddingTop">@dimen/contribution_calendar_widget_padding</item>
        <item name="android:paddingBottom">@dimen/contribution_calendar_widget_padding</item>
    </style>

    <style name="ContributionCalendarDay">
        <item name="android:layout_width">@dimen/contribution_calendar_widget_grid_size</item>
        <item name="android:layout_height">@dimen/contribution_calendar_widget_grid_size</item>
    </style>

</resources>